[package]
name = "atlas-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio.workspace = true
anyhow.workspace = true
thiserror.workspace = true
tracing.workspace = true
serde.workspace = true

atlas-common = { path = "../atlas-common" }
atlas-protocol = { path = "../atlas-protocol" }
atlas-infra = { path = "../atlas-infra" }

serde_json = "1.0"
schemars = { version = "0.8", features = ["derive"] } # 核心：Rust Struct -> JSON Schema
async-trait = "0.1"
tiktoken-rs = "0.5"
chrono = "0.4"
